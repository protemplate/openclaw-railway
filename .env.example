# MoltBot Railway Template - Environment Variables
# Copy this file to .env for local development

# ==============================================================================
# REQUIRED CONFIGURATION
# ==============================================================================

# Setup wizard password - protects the /setup endpoint
# Generate with: openssl rand -base64 32
SETUP_PASSWORD=your-secure-password-here

# ==============================================================================
# DATA PERSISTENCE (Recommended)
# ==============================================================================

# State directory for MoltBot configuration, sessions, credentials
# On Railway, this should be within your mounted volume at /data
MOLTBOT_STATE_DIR=/data/.moltbot

# Workspace directory for file storage
MOLTBOT_WORKSPACE_DIR=/data/workspace

# ==============================================================================
# OPTIONAL CONFIGURATION
# ==============================================================================

# Gateway authentication token
# If not set, the wrapper server will auto-generate one and store it
# in $MOLTBOT_STATE_DIR/gateway.token
# MOLTBOT_GATEWAY_TOKEN=your-gateway-token

# Internal gateway port (default: 18789)
# This is the port MoltBot gateway listens on internally
# INTERNAL_GATEWAY_PORT=18789

# ==============================================================================
# LOCAL DEVELOPMENT ONLY
# ==============================================================================

# Port for local development (Railway sets this automatically)
# PORT=8080
